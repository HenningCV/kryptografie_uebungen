#include <iostream>

#include "faktorisierung.h"
#include "gcd.h"

int fun(int x, int n)
{
	return (x * x + 1) % n;
}

int pollardRho(int n)
{

	return 0;
}

int fermat(int n)
{
	int y = 0;

	return y;
}